# ML service environment — copy to .env
ML_PORT=5000
# Node backend base URL (used by the /push endpoint to write scores back)
NODE_API_URL=http://localhost:4000
# Optional: increase to slow down
LOG_LEVEL=info
